function [f,f_grad,ci,ci_grad,ce,ce_grad] = combinedFunction(X,parameters)
    Y = parameters.Y;
    m = parameters.m;
    
    f = 1/m*norm(q'*X, 1);
    f_grad = 1/m*X*sign(X'*q))
    
    ci = [];
    ci_grad = [];
    
    
          
end